# ADGM-Compliant Corporate Agent

This project lets you upload `.docx` legal documents for ADGM processes, 
checks them for compliance using AI, and returns:

- Reviewed `.docx` with inline red comments
- Structured JSON summary of issues

## Setup

1. Install dependencies:
